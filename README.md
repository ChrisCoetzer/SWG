# SWG Resource Tracker

Local Electron + Vite + React desktop app. Data stored locally as JSON via Electron main process.

## Dev
npm install
npm run dev

## Build (prod assets)
npm run build

## Package (installer/exe)
npm run dist
